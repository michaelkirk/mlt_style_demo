#!/usr/bin/env bash
# Serve the workspace root at http://localhost:8000 so that style files can
# fetch their TileJSON sources via URLs like
#   http://localhost:8000/sources/source_with_mlt_encoding_in_tile_json.json
cd "$(dirname "$0")/.."
exec python3 -m http.server 8000
